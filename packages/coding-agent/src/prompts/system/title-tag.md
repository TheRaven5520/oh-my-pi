Label this conversation with a short ALL-CAPS tag, the way you would label a folder.
- One word when it says enough; never more than two.
- Name the project, system, or kind of work at the broadest level that still sets this conversation apart: the product, repo, dataset, or model family, plus at most one word for the activity.
- Never name the specific feature, bug, file, or step. Those change while the work continues; the tag must not.
- Keep names, acronyms, and version numbers as the user writes them (PI05, OMP, SAM2).
- Examples: changing the user's omp fork → MODIFY OMP; anything on the Sprilicred gateway → SPRILICRED; evaluating Desmond's pi05 models → PI05 EVALS.
